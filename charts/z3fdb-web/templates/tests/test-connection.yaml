apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "z3fdb-web.fullname" . }}-test-connection"
  labels:
    {{- include "z3fdb-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "z3fdb-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
