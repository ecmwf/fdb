from setuptools import setup

setup(
    name="z3fdb",
    version="1.0.0",
    packages=["z3fdb", "pychunked_data_view", "chunked_data_view_bindings"],
    package_data={
        "chunked_data_view_bindings": ["*.so", "*.pyd"],
    },
    install_requires=["numpy", "zarr>=3,<4", "findlibs"],
    has_ext_modules=lambda: True,
)
