#!/bin/bash

set -e

MULTIO_SINKS=${MULTIO_SINKS:=fdb4}


for sink in $(echo $MULTIO_SINKS | tr ':' ' ')
do

    if [[ "$sink" == "fdb5" ]]
    then
        echo "FDB5"
        exit
    else
        echo "$SCHOST fdb"
        exit
    fi

done

echo "fdb"
