#!/bin/bash

MULTIO_SINKS=${MULTIO_SINKS:=fdb4}

set -e

for sink in $(echo MULTIO_SINKS | tr ':' ' ')
do

    if [[ "$sink" == "fdb5" ]]
    then
        CMD="grib2fdb5"
    else
        CMD="/usr/local/apps/mars/configs/prod/bin/grib2fdb"
    fi

    $CMD "$@"
done
