---
credentials:
  - endpoint: '127.0.0.1:9000'
    accessKeyID: 'minio'
    secretKey: 'minio1234'

  - endpoint: 'minio:9000'
    accessKeyID: 'minio'
    secretKey: 'minio1234'

  - endpoint: 'localhost:9000'
    accessKeyID: 'asd2'
    secretKey: 'asd2'
