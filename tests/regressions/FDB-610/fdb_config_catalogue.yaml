type: catalogue
serverPort: 8002
engine: toc
schema: ~fdb/schema
supportedConnections: [1]
spaces:
- handler: Default
  roots:
  - path: ~fdb/db_store
stores:
  - default: "localhost:8003"
    serveLocalData: True
